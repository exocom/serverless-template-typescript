provider:
  environment:
    CHANGE_VALUE: "foo"

custom:
  customDomain:
    domainName: "dev.change.com"
    certificateName: "*.change.com"
    enabled: true
    basePathMappings:
      - path: /
        stage: dev
