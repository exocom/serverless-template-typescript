{
  "scripts": {
    "prepare-node_modules-layer": "yarn install --flat --production --modules-folder='layers/node_modules/nodejs/node8/node_modules' --no-lockfile && yarn"
  },
  "devDependencies": {
    "@kalarrs/serverless-domain-manager": "^2.4.0",
    "@kalarrs/serverless-shared-api-gateway": "^1.0.8",
    "@kalarrs/serverless-workspace-utils": "^1.0.1",
    "serverless": "^1.34.1",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "lodash": "^4.17.11",
    "moment": "^2.22.2"
  }
}
